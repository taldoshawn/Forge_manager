<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Files copied for "Abrir com" and sharing live under the private app cache.
         FileProvider still grants access only to the exact Uri handed to another app. -->
    <cache-path name="forge_cache" path="." />
    <external-path name="external_files" path="." />
</paths>
